import { describe, it, expect } from 'vitest'

import { mount } from '@vue/test-utils'
import AddCardPage from '../AddCardPage.vue'

import { data } from '../../store.js'

describe('CheckMemeStructure', () => {
  it('data of the new meme correspond to as valid format', () => {
    // TODO : tester la structure de l'objet ajouté au store après appel API.
  })
})
